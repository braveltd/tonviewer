export type Adaptable = {
  isMobile?: boolean;
  isTablet?: boolean;
  isTrace?: boolean;
  isSimplePreview?: boolean;
};

export type Activable = {
  isActive?: boolean;
  isApps?: boolean;
};

export type Themeable = {
  darkMode?: boolean;
};
